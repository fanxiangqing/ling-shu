apiVersion: v1
kind: Service
metadata:
  name: ling-shu-api
  labels:
    app.kubernetes.io/name: ling-shu
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ling-shu
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http

